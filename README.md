
# Installation

When opening the zip file, open `tp_3dme-main` directory folder and inside there will be another directory folder which is called as `tp_3dme`, this directory folder is the one that should be exported to your resources (The folder which contains `fxmanifest.lua`).
